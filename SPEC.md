# EduQuest — 초등 2~3학년 수학·국어 학습 게임 허브 (SPEC v1.0)

> Claude Code 구현용 마스터 스펙.
> 대상: 초등 2학년 (3학년 과정까지 커버), 갤럭시탭 크롬 PWA.
> 철학: "학습 앱에 게임을 붙인 것"이 아니라 "게임인데 학습이 되는 것".

---

## 0. 하드 제약 (HARD CONSTRAINTS — 위반 시 해당 페이즈 실패)

1. **오프라인 우선**: 서비스워커 필수. 최초 로드 후 인터넷 없이 전 기능 동작 (TTS 제외 — TTS는 기기 내장 엔진 사용이므로 사실상 오프라인 동작).
2. **외부 서버/백엔드 금지**: 정적 파일만으로 배포 (GitHub Pages 또는 로컬 파일). 모든 데이터는 localStorage/IndexedDB.
3. **개인정보 수집 금지**: 계정, 로그인, 서버 전송 일절 없음. 프라이버시 바이 디자인.
4. **광고/외부 링크/인앱결제 금지**: 아이가 혼자 써도 안전해야 함.
5. **게임 주스(juice) 필수 요건** (§3) 미충족 시 해당 미니게임은 미완성으로 간주.
6. **한국어 UI 전용**: 모든 텍스트, 버튼, 피드백 메시지는 한국어. 폰트는 어린이 가독성 좋은 것 (Pretendard 또는 시스템 폰트, 웹폰트는 로컬 번들).
7. **터치 우선**: 마우스 이벤트가 아닌 pointer 이벤트 기반. 최소 터치 타겟 48×48px.
8. **60fps 목표**: 갤럭시탭 중급기 기준. 파티클 수 상한 등 성능 가드 포함.
9. **페이즈 게이트**: 각 페이즈의 테스트가 전부 그린이 되기 전까지 다음 페이즈 착수 금지.
10. **부모 모드 분리**: 데이터 입력/설정은 부모 모드(간단한 잠금 — 예: "구구단 7×8=?" 성인 확인용은 과하므로 3초 길게 누르기)에서만.

---

## 1. 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 게임 엔진 | **Phaser 3.80+** | 미니게임 씬 전부 |
| 허브 UI | Vanilla JS + HTML/CSS (또는 Preact 허용) | 월드맵, 설정, 급수표 관리 화면 |
| 빌드 | Vite | 단일 dist 폴더 산출 |
| 저장 | localStorage(설정·진행도) + IndexedDB(급수표 DB, 오답노트) | |
| 음성 | Web Speech API `speechSynthesis` (ko-KR) | 갤럭시탭 삼성/구글 TTS 엔진 |
| 사운드 | Phaser Sound + 무료 라이선스 효과음 (직접 생성한 WebAudio 신디사이저 허용) | 외부 CDN 금지, 로컬 번들 |
| 테스트 | Vitest (로직) + Playwright (스모크) | |
| 배포 | PWA (manifest + service worker) | §8 |

---

## 2. 전체 구조 — "학습 게임 허브"

```
[타이틀 화면]
    └─ [월드맵]  ← 코인 잔액, 캐릭터 표시
         ├─ 🧮 수학 월드
         │     ├─ M-1 구구단 러너
         │     ├─ M-2 시계 폭탄 해제
         │     ├─ M-3 블록 연산 (받아올림/내림)
         │     └─ M-4 길이 다리 건설
         ├─ ✍️ 국어 월드
         │     ├─ K-1 받아쓰기 스튜디오 (급수표 기반) ★핵심
         │     ├─ K-2 띄어쓰기 슬래시
         │     └─ K-3 겹받침 징검다리
         ├─ 🏪 상점 (캐릭터 꾸미기 — 코인 소비처)
         └─ ⚙️ 부모 모드 (급수표 관리, 난이도, TTS 설정, 통계)
```

### 2.1 공유 경제 시스템 (크로스 동기부여)

- 모든 게임에서 **코인** 획득: 정답 +10, 콤보 보너스 ×콤보수, 스테이지 클리어 +50.
- 코인으로 상점에서 캐릭터 스킨/모자/펫 구매 (코스메틱 전용, 게임 유불리 없음).
- **일일 스트릭**: 하루 1개 이상 스테이지 클리어 시 불꽃 카운터 증가. 7일 달성 시 특별 스킨.
- 진행도 구조 (localStorage `eduquest_progress`):

```json
{
  "coins": 340,
  "streak": { "count": 5, "lastDate": "2026-07-10" },
  "unlockedSkins": ["default", "cap_red"],
  "equippedSkin": "cap_red",
  "worlds": {
    "math": { "m1": { "level": 12, "stars": 28 }, "m2": { "level": 3, "stars": 7 } },
    "korean": { "k1": { "완료회차": [1,2,3] } }
  }
}
```

### 2.2 난이도 = 레벨 곡선 (학년 아님)

학년 표기를 UI에 노출하지 않는다. 레벨이 오르면 자연스럽게 3학년 범위로 진입.
각 미니게임은 내부적으로 `curriculum.js`의 문제 생성기를 사용하며, 레벨→출제범위 매핑 테이블을 가진다 (§4의 각 게임 명세에 정의).

---

## 3. 게임 주스(JUICE) 필수 요건 — 전 미니게임 공통

> "출시된 게임 성능"의 실체. 아래 항목은 체크리스트이며 Phase 6 게이트에서 전수 검사.

| # | 요건 | 상세 |
|---|---|---|
| J1 | 정답 피드백 | 파티클 버스트(별/불꽃 15~30개) + 코인 +10 플로팅 텍스트 + 상승 효과음(콤보마다 피치 상승) |
| J2 | 콤보 시스템 | 연속 정답 시 화면 상단 콤보 카운터 + 3콤보부터 "굉장해!", 5콤보 "대단해!!" 등 한국어 칭찬 배너 |
| J3 | 오답 피드백 | 화면 흔들림(shake 150ms) + 둔탁한 효과음 + 캐릭터 비틀거림 애니메이션. **절대 벌점/조롱 금지** — 오답은 "다시 도전!"으로 즉시 재기회 |
| J4 | 화면 전환 | 씬 전환 시 페이드/와이프 300ms. 뚝뚝 끊기는 즉시 전환 금지 |
| J5 | 마이크로 애니메이션 | 모든 버튼 눌림 시 scale 0.95 바운스. 대기 중 캐릭터 idle 애니메이션(숨쉬기 2프레임 이상) |
| J6 | 스테이지 클리어 연출 | 별 1~3개 순차 등장(팡! 효과음 개별) + 코인 카운트업 롤링 + 폭죽 파티클 |
| J7 | BGM/SFX | 월드맵 BGM 1곡, 게임 중 BGM 1곡 이상, 효과음 최소 8종. 음소거 토글 제공 |
| J8 | 슬로우모션 | 러너 게임 오답/충돌 시 0.3배속 400ms 후 복귀 |
| J9 | 레벨업 연출 | 레벨 상승 시 전용 팝업 + 광선 이펙트 |
| J10 | 성능 가드 | 파티클 동시 상한 100개, 오브젝트 풀링 필수 |

효과음이 확보 어려우면 WebAudio OscillatorNode로 신디사이저 효과음 생성기(`sfx.js`)를 직접 구현할 것 (코인=상승 아르페지오, 오답=낮은 사각파 등).

---

## 4. 수학 월드 명세

### M-1 구구단 러너 ★수학 핵심

- **형식**: 횡스크롤 런 게임. 캐릭터 자동 달리기, 장애물 접근 시 문제 출현.
- **조작**: 장애물 위에 `7 × 8 = ?` 표시 → 화면 하단 4지선다 큰 버튼(오답지는 근접값: 54, 56, 63, 48). 제한시간 게이지(레벨 따라 6→3초).
- **정답**: 캐릭터가 장애물 격파(파편 파티클 + 타격 정지 프레임 2f) 후 가속.
- **오답/시간초과**: 슬로우모션 + 충돌 비틀거림, 목숨 1 감소(하트 3개), 같은 문제 유형 잠시 후 재출제.
- **레벨 곡선**:

| 레벨 | 출제 범위 |
|---|---|
| 1~4 | 2, 5, 3, 4단 (단별 순차 도입) |
| 5~8 | 6, 7, 8, 9단 |
| 9~11 | 전체 랜덤 + 역방향(`? × 7 = 56`) |
| 12~14 | 두 자리 × 한 자리 (3학년: 23×3) — 이때만 숫자 키패드 입력 |
| 15+ | 혼합 + 시간 단축 |

- **적응 출제**: 오답 문제는 가중치 3배로 재등장 (세션 내 + IndexedDB `weakness` 테이블에 누적).

### M-2 시계 폭탄 해제

- **형식**: 우주선 수리 미션. 아날로그시계 바늘을 드래그해 제시 시각 맞추기. 남은 시간 게이지 = 폭탄 도화선.
- **조작**: 분침 드래그 시 시침 자동 연동 회전 (실제 시계 기어비 구현 — 분침 360° = 시침 30°). 스냅: 레벨별 5분/1분 단위.
- **레벨 곡선**: L1~3 정각·30분 → L4~6 5분 단위 → L7~9 1분 단위 → L10+ "지금 3시 40분, 30분 뒤는?" 시간 계산(3학년), 디지털↔아날로그 변환.
- **연출**: 성공 시 폭탄이 선물상자로 변신 + 우주선 발진.

### M-3 블록 연산 (받아올림/내림)

- **형식**: 마인크래프트풍 인벤토리. 두 자리 덧셈/뺄셈을 블록으로 시각화.
- **핵심 메커닉**: 일의 자리 나무블록 10개 모이면 **자동으로 다이아 1개로 합체하는 애니메이션**(받아올림), 뺄셈에서 부족하면 다이아 1개가 나무 10개로 **쪼개지는 애니메이션**(받아내림). 이 변환 연출이 이 게임의 존재 이유 — 반드시 부드럽게(400ms, 이징) 구현.
- **진행**: ①블록 조작 모드(개념) → ②세로셈 입력 모드(숙달, 블록은 힌트 버튼) 2단계.
- **레벨 곡선**: 받아올림 없는 두 자리 → 받아올림 1회 → 받아내림 → 세 자리(3학년) → 나눗셈 기초(블록 똑같이 나누기).

### M-4 길이 다리 건설

- **형식**: 강 건너기. "다리 길이 1m 20cm가 필요해!" → 30cm, 50cm, 1m 판자 조각을 드래그로 조합.
- **레벨 곡선**: cm 단독 → cm+m 혼합·단위 변환 → mm/km 개념(3학년) → 들이(L/mL)·무게(kg/g)로 소재 확장(물통 채우기/저울 균형 변형 스테이지).
- **연출**: 완성된 다리를 캐릭터가 건너감. 길이 부족/초과 시 다리가 기울어 캐릭터 허둥지둥(익살, 공포 아님).

---

## 5. 국어 월드 명세

### K-1 받아쓰기 스튜디오 ★국어 핵심 (급수표 기반)

#### 5.1 급수표 데이터 등록 (부모 모드)

- **MVP 방식**: 부모가 Claude 챗에 급수표 사진 업로드 → 아래 JSON으로 변환받아 앱의 "급수표 붙여넣기" 텍스트영역에 붙여넣기 → 검증 후 IndexedDB 저장.
- 앱 내에 "Claude에게 보낼 변환 요청문" 복사 버튼 제공 (프롬프트 템플릿 내장):

```
다음 받아쓰기 급수표 사진을 아래 JSON 형식으로 변환해줘. 문장 원문을 절대 수정하지 말 것.
{ "title": "1학기 받아쓰기", "sets": [ { "level": 1, "items": ["문장1", "문장2"] } ] }
```

- **수동 입력**도 지원: 급수 추가 → 문장 한 줄씩 입력/편집/삭제.
- **검증기**: JSON 파싱 실패 시 줄 단위 폴백 파서(빈 줄로 급수 구분, `1.` `2.` 번호 자동 제거). 파서는 단위 테스트 필수.
- v2 예약: Anthropic API 비전 내장 OCR (API 키 설정 UI 포함) — **본 스펙 범위 외, 훅만 남길 것**.

#### 5.2 받아쓰기 진행 플로우 (아이 모드)

```
급수 선택 → [준비 화면: 연필 준비!] →
문장 1: 🔊 자동 낭독 (설정된 횟수 반복, 문장 간 휴지)
        [다시 듣기] 버튼 (무제한)
        아이는 공책에 손으로 씀
→ [다 썼어요!] 터치 → 정답 문장 크게 공개
→ 자기채점: [⭕ 맞았어요] [❌ 틀렸어요] 큰 버튼
→ 다음 문장 … → 결과 화면 (점수, 별, 코인)
```

- **자기채점 원칙**: 앱이 채점하지 않는다(손글씨이므로). 아이가 정답과 자기 글씨를 비교하는 행위 자체가 학습.
- ❌ 선택 시 해당 문장은 **오답노트**(IndexedDB)에 자동 등록.

#### 5.3 TTS 요구사항

- `speechSynthesis`, `lang: 'ko-KR'`, 사용 가능 목소리 목록에서 선택 UI (부모 모드).
- **속도**: 0.7 / 0.85 / 1.0 (기본 0.85).
- **반복 횟수**: 문장당 2 / 3회 (기본 2회), 반복 사이 간격 2초.
- **휴지 시간**: 낭독 완료 후 다음 자동 진행까지 5 / 10 / 15초 (기본 10초) — 또는 "수동 진행" 모드.
- 갤럭시탭 크롬의 `speechSynthesis` 특성 방어 코드: 첫 발화는 사용자 제스처 이후에만, `getVoices()` 비동기 로딩 대응, 15초 이상 발화 끊김 버그 대비 문장 단위 발화.

#### 5.4 오답노트 & 복습

- 오답 문장만 모은 "다시 도전" 급수 자동 생성.
- 2회 연속 ⭕ 받으면 오답노트에서 졸업(축하 연출).
- 부모 모드 통계: 급수별 정답률, 자주 틀리는 문장 Top 5.

### K-2 띄어쓰기 슬래시

- **형식**: 과일 자르기류 액션. 띄어쓰기 없는 문장이 화면을 가로지르며 흘러감 → 손가락으로 **글자 사이를 세로로 베면** 그 지점이 분리.
- **판정**: 베기 지점이 정답 경계 ±글자폭 40% 이내면 성공(쾌감 있는 참격 이펙트 + 문장이 쩍 갈라짐). 오답 위치 베기 → 문장이 꿈틀하며 튕겨냄.
- **출제**: 기본 예문 DB (2학년 수준 100문장 내장) + **급수표 문장 재활용 옵션**(K-1 데이터를 띄어쓰기 문제로 자동 변환 — 공백 제거하면 됨). 이게 커스텀 학습의 킬러 기능.
- **레벨**: 2어절 → 3~4어절 → 조사 붙임 함정("할머니께 서" 같은 오분리 유도) → 흐르는 속도 상승.

### K-3 겹받침 징검다리

- **형식**: 캐릭터가 개울을 건넘. 돌마다 2지선다: `넓다 / 넙다`. 올바른 돌 터치 → 폴짝 점프, 틀린 돌 → 돌이 가라앉고 캐릭터 풍덩(구조 튜브로 복귀, 익살 연출).
- **내장 DB**: 겹받침(닭, 흙, 삶, 넓다, 밟다, 앉다, 않다, 읽다, 짧다, 값…) + 자주 틀리는 맞춤법(돼요/되요, 왠지/웬지, 낳다/낫다) 최소 80쌍, `korean_db.js`에 오답 생성 규칙과 함께 정의.
- **연속 정답 = 배수 코인** (징검다리 콤보).

---

## 6. 페이즈별 빌드 계획 (테스트 그린 게이트)

> 각 페이즈 완료 조건: ①명시된 테스트 전부 통과 ②갤럭시탭 크롬 수동 스모크 체크리스트 통과 ③사람 승인.

### Phase 0 — 스캐폴드

- Vite + Phaser 프로젝트, 씬 라우터(허브↔게임), localStorage/IndexedDB 래퍼(`store.js`), sfx 신디사이저, 파티클/셰이크/플로팅텍스트 공용 juice 유틸(`juice.js`).
- **게이트**: store.js·sfx.js 단위 테스트 ≥ 15개 그린. 빈 씬 전환 60fps.

### Phase 1 — 허브 + 경제

- 타이틀, 월드맵, 코인/스트릭, 상점(스킨 3종), 부모 모드 잠금.
- **게이트**: 진행도 저장/복원, 스트릭 날짜 로직(자정 경계·하루 건너뜀) 테스트 ≥ 12개 그린.

### Phase 2 — M-1 구구단 러너 (첫 완성 게임 = juice 기준점)

- 러너 전체 + 레벨 1~11 + 적응 출제 + J1~J10 전 항목.
- **게이트**: 문제 생성기(범위·오답지 근접성·가중치 재출제) 테스트 ≥ 20개 그린. juice 체크리스트 10/10.
- **★사람 플레이 테스트**: 아이에게 이 게임 하나만 먼저 시켜보고 재미 피드백 반영 후 진행. 여기서 확립된 손맛이 이후 전 게임의 표준.

### Phase 3 — K-1 받아쓰기 스튜디오

- 급수표 파서(JSON+폴백), 부모 모드 CRUD, TTS 플로우, 자기채점, 오답노트.
- **게이트**: 파서 테스트 ≥ 15개(정상/오염 입력/번호 제거/빈 줄), 오답노트 졸업 로직 테스트 그린. 실기기 TTS 청취 확인.

### Phase 4 — M-2 시계 + M-3 블록 연산

- **게이트**: 시계 기어비·스냅 로직, 받아올림/내림 상태 머신 테스트 각 ≥ 12개 그린. 변환 애니메이션 실기기 확인.

### Phase 5 — M-4 길이 + K-2 띄어쓰기 + K-3 징검다리

- **게이트**: 단위 변환 계산기, 베기 판정 기하 로직, 겹받침 DB 무결성(중복/오답쌍 검증) 테스트 그린.

### Phase 6 — 통합 폴리시 + PWA

- 전 게임 juice 전수 검사, BGM, manifest + 서비스워커(오프라인 전체 동작), 로딩 화면, 아이콘.
- **게이트**: Playwright 스모크(허브→각 게임 진입→클리어→코인 반영) 그린. 비행기모드 전체 플로우 통과. Lighthouse PWA 체크 통과.

---

## 7. 파일 구조 (안)

```
src/
  main.js            # 부트스트랩
  store.js           # localStorage/IndexedDB 래퍼
  juice.js           # 파티클·셰이크·플로팅텍스트·콤보 공용
  sfx.js             # WebAudio 신디사이저 효과음
  tts.js             # ko-KR TTS 래퍼 (방어 코드 포함)
  curriculum/
    math_gen.js      # 문제 생성기 (레벨→범위 매핑, 오답지 생성, 가중치)
    korean_db.js     # 겹받침/맞춤법 쌍, 기본 예문 100문장
    gugupyo_parser.js# 급수표 JSON+폴백 파서
  scenes/
    Title.js  WorldMap.js  Shop.js  Parent.js
    m1_runner/  m2_clock/  m3_blocks/  m4_bridge/
    k1_dictation/  k2_slash/  k3_stones/
  assets/ (sprites, audio — 로컬 번들만)
tests/  (vitest)
e2e/    (playwright)
```

## 8. 배포

- 기본: `npm run build` → GitHub Pages (또는 로컬 http 서버) → 갤럭시탭 크롬 접속 → "홈 화면에 추가" → 전체화면 standalone PWA.
- 오프라인: 최초 1회 로드 후 비행기모드 동작 보장.
- (옵션, 스펙 외) 이전 영어 앱과 동일 경로로 APK 래핑이 필요하면 PWABuilder/TWA로 후처리 가능 — 코드 변경 불필요.

## 9. 금지 사항 재확인

- 외부 CDN 로딩, 광고, 분석 스크립트, 서버 통신, 영어 UI, 벌점형 피드백, 학년 표기 노출.

---

*승인 후 Phase 0부터 착수. 각 페이즈 종료 시 테스트 결과와 스모크 체크리스트를 보고하고 사람 승인을 대기할 것.*
