# EduQuest 구현 계획 (Implementation Plan v1.0)

> 대상 스펙: SPEC v1.0 — 초등 2~3학년 수학·국어 학습 게임 허브
> 본 문서는 스펙을 "무엇을 어떤 순서로, 어떤 구조로, 어떤 검증 기준으로" 만들지로 번역한 실행 계획이다.
> 각 페이즈는 **테스트 전부 그린 + 실기기 스모크 + 사람 승인**을 통과해야 다음 페이즈에 착수한다 (하드 제약 #9).

---

## 1. 전체 아키텍처 결정

### 1.1 하이브리드 구조: DOM 허브 + Phaser 게임

스펙의 "허브 UI는 Vanilla JS, 미니게임은 Phaser"를 다음과 같이 구현한다.

```
index.html
 ├─ <div id="hub">      ← DOM 레이어: 타이틀/월드맵/상점/부모모드 (Vanilla JS)
 └─ <div id="game">     ← Phaser 캔버스: 미니게임 진입 시에만 표시
```

- **단일 Phaser.Game 인스턴스**를 앱 수명 동안 유지하고, 미니게임 진입 시 해당 씬만 `scene.start()`. 게임 종료 시 씬 정지 + 캔버스 숨김 + DOM 허브 복귀. (매번 Game을 생성/파괴하면 갤럭시탭에서 WebGL 컨텍스트 누수 위험이 크므로 금지.)
- 허브↔게임 통신은 `router.js`의 이벤트 버스 하나로 통일:
  - `router.launch(gameId, {level})` → Phaser 씬 시작
  - 게임 종료 시 `router.exit({coins, stars, levelUp})` → 허브가 진행도 반영 후 월드맵 렌더
- Phaser 설정: `Phaser.AUTO`, `scale: FIT + CENTER_BOTH`, 기준 해상도 **1280×800(가로 고정)** — 갤럭시탭 비율. `roundPixels: true`.

### 1.2 상태/저장 계층 (`store.js`)

| 데이터 | 저장소 | 키/스토어 |
|---|---|---|
| 코인, 스트릭, 스킨, 게임별 레벨·별 | localStorage | `eduquest_progress` (스펙 §2.1 JSON) |
| 설정 (TTS 속도/반복/휴지, 음소거, 목소리) | localStorage | `eduquest_settings` |
| 급수표 DB | IndexedDB | `gugupyo` 스토어 |
| 오답노트 (받아쓰기) | IndexedDB | `mistakes` 스토어 |
| 약점 가중치 (수학 적응 출제) | IndexedDB | `weakness` 스토어 |

- localStorage 래퍼는 **스키마 버전 + 마이그레이션 훅** 포함 (`{v: 1, data: {...}}`). 파싱 실패 시 기본값 폴백 — 아이 기기에서 절대 크래시하지 않는다.
- IndexedDB는 idb 같은 외부 라이브러리 없이 ~100줄 Promise 래퍼 직접 작성 (오프라인·무의존성 원칙).
- 쓰기는 트랜잭션 단위 헬퍼 제공: `store.addCoins(n)`, `store.recordStar(gameId, level, stars)`, `store.touchStreak(today)` — 스트릭 날짜 로직(자정 경계, 하루 건너뜀 시 리셋, 같은 날 중복 무시)은 **순수 함수로 분리**해 단위 테스트.

### 1.3 공용 게임 필 계층

- **`sfx.js`** — WebAudio OscillatorNode 신디사이저. AudioContext는 첫 사용자 제스처에서 `resume()`. 최소 프리셋: `coin`(상승 아르페지오), `combo(n)`(피치가 콤보수에 비례 상승), `wrong`(낮은 사각파), `pop`, `star`, `levelup`, `tick`, `whoosh`, `splash`, `slice` — 8종 이상 충족. 외부 오디오 파일 0개로 시작하고, Phase 6에서 BGM만 로컬 번들 추가.
- **`bgm.js`** — WebAudio 기반 루프 시퀀서(신디 BGM) 또는 로컬 ogg 번들. 음소거 토글은 sfx/bgm 공통 마스터 게인 하나로.
- **`juice.js`** — Phaser 씬에 주입하는 공용 유틸 (J1~J10의 단일 구현체):
  - `burst(x, y, {count})` — 파티클 버스트. **전역 파티클 카운터로 동시 100개 상한**(J10), 초과분은 count 자동 축소. 이미터는 씬당 1개 재사용(오브젝트 풀링).
  - `floatText(x, y, '+10')` — 플로팅 텍스트 풀.
  - `shake(150)` — 카메라 셰이크.
  - `combo.hit() / combo.reset()` — 콤보 카운터 + 3/5/7콤보 칭찬 배너("굉장해!", "대단해!!", "최고야!!!").
  - `slowmo(0.3, 400)` — `scene.time.timeScale` + tween timeScale 조작 (J8).
  - `stageClear({stars, coins})` — 별 순차 등장 + 코인 롤링 카운트업 + 폭죽 (J6). **전 게임이 이 하나의 클리어 씬(오버레이)을 공유**한다.
  - `transition(scene, next)` — 300ms 페이드 (J4).
- **`ui.js`** (DOM 측) — 48×48px 이상 터치 타겟 보장 버튼 팩토리, 눌림 scale 0.95 바운스(J5), pointer 이벤트 전용.

### 1.4 TTS 계층 (`tts.js`)

갤럭시탭 크롬 방어 코드를 전부 이 모듈에 격리:

- `init()` — 첫 사용자 제스처에서 무음 발화 1회로 엔진 웜업.
- `getVoices()` — `voiceschanged` 이벤트 + 폴링(200ms×25회) 이중 대기, ko-KR 필터.
- `speak(text, {rate, voice})` — **문장 단위 발화**(15초 끊김 버그 회피), 발화 전 `cancel()`로 큐 청소, `onend` 미발화 대비 타임아웃 폴백(예상 발화시간×2), Promise 반환.
- `speakRepeated(text, {times, gapMs})` — 반복 낭독 시퀀스, 중단 가능(`AbortSignal`).
- TTS 불가 기기 감지 시 받아쓰기 화면에 "소리를 지원하지 않아요 — 부모님이 직접 읽어주세요" 폴백 모드(문장이 부모에게만 보이는 카드 뒤집기 UI).

### 1.5 출제 로직 계층 (`curriculum/`)

- `math_gen.js` — **순수 함수만** (Phaser 의존 0): `genGugudan(level, weakness)`, `genClock(level)`, `genBlock(level)`, `genLength(level)`. 오답지 생성기는 "근접값" 규칙(같은 단의 이웃 답, ±자릿수 실수값)을 명시적으로 구현. 가중치 재출제는 `weakness` 맵을 받아 확률 3배 가중 샘플링.
- `korean_db.js` — 겹받침/맞춤법 80쌍 + 기본 예문 100문장을 **데이터 상수 + 무결성 검증 함수**(중복 검사, 정답≠오답 검사)로 정의. 검증 함수는 그대로 단위 테스트가 된다.
- `gugupyo_parser.js` — 급수표 파서: ① `JSON.parse` 시도 → 스키마 검증 → ② 실패 시 줄 단위 폴백(빈 줄 = 급수 구분, `1.` `1)` `①` 등 번호 접두 제거, 공백 정리). 순수 함수.

### 1.6 PWA / 빌드

- Vite 멀티 없음 — 단일 엔트리, `base: './'` (GitHub Pages 하위 경로 + 로컬 file 서버 겸용).
- 서비스워커는 **수제 작성**(Workbox 미사용, 의존 최소화): 프리캐시 매니페스트를 빌드 후 스크립트로 생성(`scripts/gen-sw-manifest.mjs`), 캐시 우선 전략, 버전 키 갱신 시 구캐시 삭제.
- Phaser는 npm 의존으로 번들 (CDN 금지). 폰트는 Pretendard subset(woff2) 로컬 번들, 실패 시 시스템 폰트 폴백.

---

## 2. 파일 구조 (확정안)

스펙 §7 기반 + 계획상 추가분(굵게):

```
├─ index.html
├─ manifest.webmanifest
├─ sw.js                       # 수제 서비스워커 (빌드 시 프리캐시 목록 주입)
├─ vite.config.js
├─ scripts/gen-sw-manifest.mjs # dist 스캔 → sw 프리캐시 목록 생성
├─ public/assets/              # 스프라이트, 폰트, (Phase 6) BGM — 전부 로컬
├─ src/
│  ├─ main.js                  # 부트스트랩: store 로드 → 허브 렌더 → Phaser 지연 초기화
│  ├─ router.js                # 허브↔게임 씬 라우터 + 이벤트 버스     ★추가
│  ├─ store.js                 # localStorage + IndexedDB 래퍼
│  ├─ streak.js                # 스트릭 날짜 순수 로직                ★추가(테스트 용이성)
│  ├─ juice.js  ├─ sfx.js  ├─ bgm.js  ├─ tts.js
│  ├─ ui.js                    # DOM 공용 위젯(버튼/모달/토글)        ★추가
│  ├─ curriculum/
│  │  ├─ math_gen.js  ├─ korean_db.js  ├─ gugupyo_parser.js
│  │  └─ mistakes.js           # 오답노트 등록/졸업(2연속 ⭕) 로직    ★추가
│  ├─ hub/                     # DOM 화면들                          ★추가
│  │  ├─ title.js  ├─ worldmap.js  ├─ shop.js  ├─ parent.js
│  └─ scenes/                  # Phaser 씬
│     ├─ Boot.js  ├─ StageClear.js(공용 오버레이)
│     ├─ m1_runner/  m2_clock/  m3_blocks/  m4_bridge/
│     └─ k1_dictation/  k2_slash/  k3_stones/
├─ tests/                      # Vitest — 순수 로직만 (Phaser 미로딩)
└─ e2e/                        # Playwright — 스모크
```

주의: K-1 받아쓰기는 액션성이 없으므로 **DOM으로 구현**하는 게 자연스럽다(큰 텍스트, 접근성, TTS 버튼). Phaser 씬이 아니라 `hub/` 계열 화면으로 만들되 파일 위치는 `scenes/k1_dictation/`에 유지하고 결과 화면에서만 juice(파티클 캔버스 오버레이) 공유.

---

## 3. 페이즈별 실행 계획

### Phase 0 — 스캐폴드 (기반 계층)

**산출물**
1. Vite + Phaser 프로젝트 초기화, `index.html`(hub/game 이중 레이어), 1280×800 FIT 스케일.
2. `store.js`(localStorage 스키마 v1 + 마이그레이션 + IndexedDB 래퍼), `streak.js`.
3. `sfx.js` 신디사이저 10 프리셋 + 마스터 음소거.
4. `juice.js` 전체 유틸 + 파티클 풀/상한 가드.
5. `router.js` — 빈 데모 씬 2개 간 페이드 전환.
6. Vitest/Playwright 설정, GitHub Actions CI(테스트 실행)까지.

**테스트 게이트 (≥15개)**
- store: 저장/복원 라운드트립, 오염된 JSON 폴백, 스키마 마이그레이션, coins 증감, IndexedDB CRUD (fake-indexeddb 사용) — 8개+
- streak: 연속일 증가 / 하루 건너뜀 리셋 / 같은 날 중복 / 자정 경계(로컬 날짜 문자열 비교) — 4개+
- sfx: 프리셋 존재·파라미터 유효성, 음소거 시 게인 0 — 3개+
- 수동 스모크: 갤럭시탭에서 빈 씬 전환 60fps (크롬 DevTools FPS 미터)

### Phase 1 — 허브 + 경제

**산출물**
1. 타이틀 화면(터치로 시작 — 이 제스처에서 AudioContext/TTS 웜업).
2. 월드맵: 수학/국어 월드 카드, 게임 7종 진입 버튼(미구현 게임은 "공사중" 잠금 표시), 코인 잔액, 스트릭 불꽃, 장착 스킨 표시.
3. 상점: 스킨 3종(코인 소비, 구매→장착, 잔액 부족 처리), 코스메틱 전용.
4. 부모 모드: **3초 길게 누르기 잠금**(pointerdown~up 타이밍 + 진행 링 표시), 설정 골격(음소거, TTS 자리), 통계 자리.
5. 스테이지 클리어 → 코인/스트릭 반영 파이프라인 (데모 씬으로 검증).

**테스트 게이트 (≥12개)**: 진행도 저장/복원, 스킨 구매 상태 머신(잔액 부족/중복 구매/장착 전환), 스트릭 7일 보상 스킨 지급, 길게 누르기 타이머 로직.

### Phase 2 — M-1 구구단 러너 ★juice 기준점

**구현 순서** (이 게임에서 확립한 패턴이 전 게임의 표준이 되므로 가장 공들일 것):
1. `math_gen.js` 구구단 파트 먼저 — 레벨→범위 매핑 테이블(스펙 §4 표 그대로), 근접 오답지 3개 생성(같은 단 이웃 곱, 피연산자 ±1 곱, 자리 바꿈 — 중복/정답 충돌 제거), 역방향(`?×7=56`) 문제형, weakness 가중 샘플링(오답 시 가중치 3, 정답 시 1로 감쇠).
2. 러너 씬: 자동 스크롤 배경(타일 2장 순환), 캐릭터 달리기 애니메이션, 장애물 스폰 → 접근 시 문제 패널 + 4지선다 버튼 + 제한시간 게이지(레벨별 6→3초).
3. 정답: 히트스톱 2프레임 + 격파 파티클 + 가속 / 오답·시간초과: slowmo 0.3×400ms + 비틀거림 + 하트 감소 + 같은 유형 재큐잉.
4. 레벨 12~14 두자리×한자리 모드: 4지선다 대신 숫자 키패드 입력 UI.
5. 하트 3 소진 → 게임오버(벌점 없음, "다시 도전!" 즉시 재시작) / 스테이지 클리어 → 공용 StageClear + 코인/별/레벨 반영.

**테스트 게이트 (≥20개)**: 레벨별 출제 범위 준수(1~15+ 전 구간), 오답지 4개 유일성·정답 포함·근접성(정답과의 거리 통계), 역방향 문제 정답 검증, 키패드 모드 전환 임계, weakness 3배 가중 재출제(고정 시드 RNG로 분포 검증 — **모든 생성기는 주입 가능한 RNG 사용**), 시간 게이지 레벨 스케일.
**+ juice 체크리스트 J1~J10 전 항목 수동 검사, ★아이 플레이 테스트 → 피드백 반영 후 승인 대기.**

### Phase 3 — K-1 받아쓰기 스튜디오

**구현 순서**:
1. `gugupyo_parser.js` + 테스트 (코어 로직 선행).
2. 부모 모드 급수표 관리: 붙여넣기 텍스트영역 + 검증 결과 미리보기(급수/문장 수) + "Claude 변환 요청문 복사" 버튼(스펙 템플릿 내장) + 수동 CRUD(급수 추가, 문장 추가/편집/삭제) + IndexedDB 저장. v2 OCR용 훅: 설정 스키마에 `apiKey` 필드와 진입점 주석만 예약, UI 미노출.
3. 아이 모드 플로우: 급수 선택 → 준비 화면 → 문장별 [자동 낭독(반복 n회, 간격 2초) → 다시 듣기 무제한 → "다 썼어요!" → 정답 대공개 → ⭕/❌ 자기채점] → 결과 화면(점수/별/코인). 자동 진행 휴지(5/10/15초) 또는 수동 진행 모드.
4. TTS 설정 UI(부모 모드): 목소리 선택, 속도 0.7/0.85/1.0, 반복 2/3회, 휴지 시간 — 미리듣기 버튼.
5. 오답노트: ❌ 시 자동 등록 → "다시 도전" 가상 급수 자동 생성 → 2연속 ⭕ 졸업 + 축하 연출. 부모 통계: 급수별 정답률, 오답 Top 5.

**테스트 게이트**: 파서 ≥15개(정상 JSON / 스키마 위반 / 깨진 JSON→폴백 / `1.` `1)` `①` 번호 제거 / 빈 줄 급수 구분 / 공백·개행 오염 / 빈 입력 / 중복 문장), 오답노트 등록·2연속 졸업·리셋(⭕→❌→⭕⭕) 로직, 통계 집계. **실기기 TTS 청취 확인 필수.**

### Phase 4 — M-2 시계 + M-3 블록 연산

**M-2 시계 폭탄 해제**:
- 코어: `clock_logic.js` 순수 함수 — 각도↔시각 변환, 기어비(분침 각도 → 시침 = 시×30 + 분×0.5), 스냅(5분/1분), 정답 판정(허용 오차), "n분 뒤" 계산 문제 생성.
- 드래그: 분침만 드래그 가능(pointer 각도 추적), **누적 회전 추적**으로 12시 경계 넘어갈 때 시침 연동 (atan2 랩어라운드 처리 주의).
- 연출: 도화선 게이지, 성공 시 폭탄→선물상자 변신 + 우주선 발진.

**M-3 블록 연산**:
- 코어: `carry_machine.js` 순수 상태 머신 — 자리별 블록 수 상태, `add/remove/merge10/split10` 액션, 받아올림·내림 트리거 조건, 세로셈 입력 채점.
- 씬: 인벤토리 그리드, 나무10→다이아 합체 / 다이아→나무10 분해 애니메이션(400ms 이징 — **이 연출이 게임의 존재 이유이므로 tween 체인 + 파티클로 최우선 폴리시**), ①블록 모드 ②세로셈 모드(블록은 힌트 버튼).

**테스트 게이트**: 시계 기어비·스냅·랩어라운드·시간계산 ≥12개, 캐리 상태 머신(받아올림 0/1회, 받아내림, 세 자리, 나눗셈 분배) ≥12개. 변환 애니메이션 실기기 확인.

### Phase 5 — M-4 길이 + K-2 띄어쓰기 + K-3 징검다리

- **M-4**: `length_logic.js` — 단위 정규화(mm/cm/m/km, mL/L, g/kg), 목표치 대비 조합 합산·판정, 레벨별 문제 생성. 씬: 판자 드래그 스냅 조립, 부족/초과 시 다리 기울어짐 익살 연출, 들이/무게 변형 스테이지(물통 채우기, 저울 균형).
- **K-2**: `slash_logic.js` — 문장→정답 경계 인덱스 계산(공백 제거 + 원문 공백 위치 기록), 베기 x좌표→글자 경계 판정(±글자폭 40%). 씬: 흐르는 문장, 세로 스와이프 인식(pointer 궤적의 수직성 판정), 참격 이펙트 + 문장 쩍 갈라짐 / 오답 꿈틀 튕김. 출제: 내장 100문장 + **급수표 재활용 토글**(K-1 IndexedDB 데이터를 공백 제거해 변환).
- **K-3**: `korean_db.js`의 80쌍 + 오답 생성 규칙. 씬: 징검다리 2지선다, 정답 폴짝/오답 풍덩+구조튜브, 연속 정답 배수 코인.

**테스트 게이트**: 단위 변환 계산기(왕복 변환, 혼합 합산, 판정 경계), 베기 판정 기하(경계 정중앙/±39%/±41%, 다중 경계 문장), 겹받침 DB 무결성(80쌍 이상, 중복 없음, 정답≠오답, 급수표→띄어쓰기 변환 왕복) 전부 그린.

### Phase 6 — 통합 폴리시 + PWA

1. 전 게임 J1~J10 전수 체크리스트 표 작성·검사 (게임 7 × 10항목).
2. BGM: 월드맵 1곡 + 인게임 1곡 이상 (신디 루프 또는 CC0 로컬 번들), 음소거 토글 최종 배선.
3. PWA: manifest(한국어 이름, 아이콘 192/512, standalone, landscape), 수제 sw.js 프리캐시(폰트·에셋 포함 전량), 업데이트 감지 시 "새 버전이 있어요" 토스트.
4. 로딩 화면(에셋 프리로드 진행 바), 앱 아이콘.
5. 레벨업 연출(J9) 전 게임 공통 팝업 최종 통일.
6. GitHub Pages 배포 워크플로(`gh-pages` 브랜치 또는 Actions).

**테스트 게이트**: Playwright 스모크 — 허브 로드 → 각 게임 7종 진입 → (테스트 훅으로) 클리어 → 코인 반영 → 복귀. 오프라인 시뮬레이션(Playwright `context.setOffline(true)` 재로드) 통과. 실기기 비행기모드 전체 플로우. Lighthouse PWA 통과.

---

## 4. 테스트 전략 요약

- **Vitest 대상은 순수 로직만**: curriculum 생성기 전부, 파서, 스트릭, 상점 상태 머신, 시계/캐리/단위/베기 판정, 오답노트. Phaser는 로딩하지 않는다 → 로직을 씬 밖 순수 모듈로 강제 분리하는 설계 규율이 된다.
- **모든 랜덤은 주입 RNG**(mulberry32 등) — 분포·재출제 가중치를 결정론적으로 검증.
- **IndexedDB 테스트는 fake-indexeddb** (devDependency, 번들 미포함이므로 CDN 금지 위반 아님).
- **Playwright는 스모크 전용**: 화면 진입·클리어·저장 반영·오프라인. 게임플레이 내부는 각 씬이 노출하는 테스트 훅(`window.__eduquest.forceClear()` — 프로덕션 빌드에서 debug 플래그로만 활성)으로 단축.
- CI: push마다 vitest + build + playwright(chromium).

## 5. 주요 리스크와 대응

| 리스크 | 대응 |
|---|---|
| 갤럭시탭 크롬 TTS 불안정 (getVoices 지연, onend 미발화, 장문 끊김) | Phase 0~1에서 미리 `tts.js` 프로토타입을 실기기 검증. 타임아웃 폴백 + 문장 단위 발화 + 부모 낭독 폴백 모드 |
| 60fps 미달 | 파티클 상한·풀링을 Phase 0에 내장, 각 페이즈 스모크에 FPS 항목 포함. 저사양 감지 시 파티클 50% 축소 플래그 |
| Phaser + DOM 이중 레이어 포커스/터치 간섭 | 활성 레이어만 `pointer-events` 허용, 비활성 레이어 `display:none` |
| 시계 드래그 랩어라운드 버그 | 누적 각도 추적 방식 + 경계 케이스 단위 테스트 선행 |
| 급수표 붙여넣기 실패로 부모 이탈 | 폴백 파서 + 실패 시 "어느 줄이 문제인지" 한국어 안내 + 수동 입력 항시 제공 |
| 서비스워커 캐시 갱신 누락으로 구버전 고착 | 빌드 해시 기반 캐시 키 + 업데이트 토스트 |
| 스코프 과대 (게임 7종 + PWA) | 페이즈 게이트 엄수. Phase 2(러너) 완성도가 기준점 — 이후 게임은 juice.js 재사용으로 비용 급감 |

## 6. 일정 감각 (작업 단위 기준)

| 페이즈 | 상대 규모 | 비고 |
|---|---|---|
| P0 스캐폴드 | ●●○ | 기반 품질이 이후 전부를 좌우 |
| P1 허브+경제 | ●●○ | |
| P2 구구단 러너 | ●●●● | 최대 투자 — juice 표준 확립 + 아이 테스트 |
| P3 받아쓰기 | ●●● | TTS 실기기 검증 왕복 포함 |
| P4 시계+블록 | ●●● | 게임 2종이지만 juice 재사용 |
| P5 길이+슬래시+징검다리 | ●●● | 게임 3종, 로직 비중 큼 |
| P6 통합+PWA | ●●○ | 전수 검사 + 배포 |

---

## 7. 착수 순서 (승인 후 첫 작업)

1. `npm create vite` 스캐폴드 + Phaser/Vitest/Playwright/fake-indexeddb 설치, CI 워크플로.
2. `store.js` + `streak.js` + 테스트 (가장 많은 모듈이 의존하는 기반).
3. `sfx.js` + `juice.js` + 데모 씬으로 실기기 60fps 확인.
4. Phase 0 게이트 보고 → 승인 대기.
